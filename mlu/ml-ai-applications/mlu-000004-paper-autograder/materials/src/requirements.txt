# Faculty AI Seminar — Paper Autograding Lab dependencies
#
# Same pinned stack as the participation and curriculum labs (Amazon Bedrock +
# Nova Lite + Nova Multimodal Embeddings + FAISS + LangChain), plus python-docx
# so student papers submitted as .docx can be read. Versions chosen for the
# SageMaker Studio Python 3 (Data Science) kernel.
#
# Verify by running paper-grading-lab.ipynb end-to-end on the bundled sample
# after install.

boto3>=1.35.0,<2.0
langchain>=0.3.0,<0.4
langchain-aws>=0.2.0,<0.3
langchain-community>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
faiss-cpu>=1.9.0,<2.0
pypdf>=5.0.0,<6.0
python-docx>=1.1.0,<2.0

# Local option: run the grader on your own machine via Ollama instead of
# Bedrock (set PROVIDER = "ollama" in Part 1). Harmless to install on the
# Bedrock/SageMaker path; needed only for the local path.
langchain-ollama>=0.2.0,<0.4


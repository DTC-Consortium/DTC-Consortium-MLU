# EXAMPLE — ML/AI Application Template

> ## This is a template, not a published contribution
>
> Copy this folder to `mlu/ml-ai-applications/<your-id>-<short-slug>/`, then replace everything in it.
> Nothing here is real teaching material.

**`mlu-000000`** · ML/AI Applications · v0.1.0 · pilot

One paragraph: what this is, what problem it addresses, and what someone ends up with. Write it for
a colleague in another department who has never met you.

## At a glance

| | |
|---|---|
| **Audience** | Who it is for |
| **Duration** | How long it takes |
| **Delivery** | In person, online, self-paced |
| **Runs on** | Software, services, or nothing at all |
| **Prerequisites** | Accounts and prior knowledge needed |
| **Expected cost** | Per participant, honestly |
| **Sample data** | What ships with it, and whether it runs out of the box |
| **You end with** | The concrete thing a participant walks away with |

## Learning outcomes

<!-- For an application, replace this heading with "Intended use" and describe what the tool
     does and what the human must still decide. -->

By the end, someone can:

1. Do a specific, checkable thing.
2. And another.

## Contents

| Path | What it is |
|---|---|
| `materials/...` | Replace with a real table of what is here |

**Delete the folders under `materials/` that you do not use.** Keep runnable things together: if a
notebook loads data by relative path, do not scatter the data to match the suggested folder names.

## How to use it

1. **Download** this contribution — see [Downloading a contribution](../../README.md#downloading-a-contribution).
2. Say what a facilitator does before delivery.
3. Say what a participant does.

## Reuse and attribution

Documentation is `CC-BY-4.0`; code is `MIT`; bundled data keeps its own per-file licence. See
`LICENSE.md` for the file-by-file table and `CITATION.cff` for how to cite this work.

**`LICENSE.md` and `CITATION.cff` are generated** from `mlu-contribution.yml` by
`scripts/generate_boilerplate.py`. Do not write them by hand.

Structured metadata lives in `mlu-contribution.yml`.
